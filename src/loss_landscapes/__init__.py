from loss_landscapes.main import linear_interpolation, planar_interpolation, point, random_line, random_plane
from loss_landscapes.model_interface.model_wrapper import GeneralModelWrapper, ModelWrapper
